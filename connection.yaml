# Template for connection.yaml
# TODO: Set up BLE on the ESP32 and find its address.

device:
  name: ESP32-PressurePad
  address: "AA:BB:CC:DD:EE:FF"  # Optional if scanning by name

ble:
  service_uuid: "12345678-1234-5678-1234-56789abcdef0"
  characteristic_uuid: "abcdef12-3456-7890-abcd-ef1234567890"

notes: |
  This is the ESP32 GATT profile for the pressure sensing mat.
  It sends a comma-separated string of values (row-major) over the notify characteristic.
